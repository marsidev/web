export const remToPx = (units: number) => units * 16

export const chakraUnitToRem = (units: number) => units * 0.25

export const chakraUnitToPx = (units: number) => units * 4
// const chakraUnitToPx = (units: number) => remToPx(chakraUnitToRem(units))
